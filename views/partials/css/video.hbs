<style>
    i {
        width: 2rem;
        font-size: 3rem;
        cursor: pointer;
        transition: 0.2s;
    }

    i:hover {
        transform: scale(1.2);
    }

    .rtl {
        direction: rtl;
        text-align: right;
    }

    .controls {
        opacity: 0;
        transition: 0.5s;
    }

    #top-control {
        background: linear-gradient(to bottom, black, transparent);
    }

    #video-control {
        background: linear-gradient(to top, black, transparent);
    }

    input[type=range]::-webkit-slider-runnable-track {
        background-color: transparent;
    }

    input[type=range]::-webkit-slider-thumb {
        width: 1.5rem;
        height: 1.5rem;
        margin-top: -0.5rem;
        background-color: white !important;
        border: 1px solid white;
        outline: 2px solid white;
        outline-offset: -1px;
    }

    #video-progress {
        width: 98%;
        height: 5px;
        background: rgba(255, 255, 255, 0.5);
        border-radius: 0;
        align-self: center;
        transition: 0.2s;
    }

    #video-progress::-webkit-slider-thumb {
        background-color: red !important;
        border-color: red;
        outline-color: red;
        opacity: 0;
        transition: 0.2s;
    }

    #video-progress:hover {
        height: 8px;
    }

    #video-progress:hover::-webkit-slider-thumb {
        opacity: 1;
    }

    #audio-range {
        width: 0;
        height: 5px;
        margin-top: -0.5rem;
        background: white;
        opacity: 0;
        transition: 0.2s;
        pointer-events: none;
    }

    .audio-range-active {
        width: 6% !important;
        opacity: 1 !important;
        pointer-events: all !important;
    }

    #play-pause-animation {
        width: 7rem;
        height: 7rem;
        background-color: rgba(0, 0, 0, 0.5);
        opacity: 0;
        animation: flash_play_pause 0.5s ease-in-out;
    }

    @keyframes flash_play_pause {
        from {
            opacity: 1;
        }

        to {
            opacity: 0;
        }
    }

    #playback-speed {
        right: 0;
        bottom: 6.5rem;
        opacity: 0;
        transition: 0.2s;
    }

    #playback-speed h2 {
        margin-left: -1rem;
    }

    #playback-speed .d-flex {
        width: 40rem;
        justify-content: space-between;
        align-items: center;
    }

    #playback-speed hr {
        bottom: 48%;
        border: 1px solid rgb(160, 160, 160);
        opacity: 1;
    }

    .speed-button {
        position: relative;
        width: 15px;
        height: 15px;
        background-color: rgb(160, 160, 160);
        cursor: pointer;
        box-sizing: content-box;
    }

    .speed-button-active {
        background-color: white;
        border: 7px solid rgb(var(--bs-dark-rgb));
        outline: 3px solid rgb(160, 160, 160);
        transform: scale(1.5);
    }
</style>