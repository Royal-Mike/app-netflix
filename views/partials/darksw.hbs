<script>
    $(() => {
        $("#sw-theme").on("click", function() {
            let theme = $("body").attr("data-bs-theme") === "light" ? "dark" : "light";
            $("body").attr("data-bs-theme", theme);
            $(this).toggleClass("fa-moon fa-sun");
            Cookies.set("theme", theme, { expires: 365 });
        });
    });
</script>